<project name="SimpleAutomaticBuildProject">
  <property file = "build.properties"/>

  <target name="init">
    <mkdir dir="${lib.dir}"/>
    <mkdir dir="${bin.dir}"/>
    <mkdir dir="${dist.dir}"/>
  </target>  

</project>